const {bearer,verifyToken}=require("../lib/pi");const store=require("../lib/store");const game=require("../lib/game-engine");
module.exports=async function(req,res){res.setHeader("Cache-Control","no-store");if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{const token=bearer(req);if(!token)return res.status(401).json({error:"Missing Pi access token"});const user=await verifyToken(token);if(!store.configured())return res.status(503).json({error:"Persistent store unavailable"});let state=await store.getGame(user.uid);if(!state||state.v!==4)state=game.fresh();const hasBoost=await store.hasBoost(user.uid,state.era);state=game.advance(state,Date.now(),hasBoost);await store.saveGame(user.uid,state);return res.status(200).json({ok:true,uid:user.uid,username:user.username||null,state:game.summary(state,hasBoost),hasBoost,storeReady:true});}catch(e){const status=/Unauthorized|access token/i.test(e?.message||"")?401:500;return res.status(status).json({error:e.message||"Authentication failed"});}};