const {bearer,verifyToken}=require("../lib/pi");const store=require("../lib/store");
module.exports=async function(req,res){if(!["GET","POST"].includes(req.method))return res.status(405).json({error:"Method not allowed"});try{const token=bearer(req);if(!token)return res.status(401).json({error:"Missing Pi access token"});const user=await verifyToken(token);if(!store.configured())return res.status(503).json({error:"Persistent store unavailable"});if(req.method==="GET")return res.status(200).json({state:await store.getGame(user.uid)});const state=await store.saveGame(user.uid,req.body||{});return res.status(200).json({ok:true,state});}catch(e){return res.status(400).json({error:e.message||"Game state error"});}};